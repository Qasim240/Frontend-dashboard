(function($) {
  "use strict";

  
  var isSidebarOpen = false;
  var isScrolling = false;

  $("#sidebarToggle, #sidebarToggleTop").on("click", function(e) {
    $("body").toggleClass("sidebar-toggled");
    $(".sidebar").toggleClass("toggled");
    isSidebarOpen = $(".sidebar").hasClass("toggled");
    if (isSidebarOpen) {
      $(".sidebar .collapse").collapse("hide");
    }
  });

  $(window).scroll(function() {
    if (isSidebarOpen && !isScrolling) {
      isScrolling = true;
      $(".sidebar").addClass("toggled");
      setTimeout(function() {
        isScrolling = false;
      }, 200);
    }
  });
  
    $(window).resize(function() {
      if ($(window).width() < 768) {
        $(".sidebar .collapse").collapse("hide");
      }
      // if ($(window).width() < 480 && !$(".sidebar").hasClass("toggled")) {
      //   $("body").addClass("sidebar-toggled");
      //   $(".sidebar").addClass("toggled");
      //   $(".sidebar .collapse").collapse("hide");
      // }
    });
  
    $("body.fixed-nav .sidebar").on("mousewheel DOMMouseScroll wheel", function(e) {
      if ($(".sidebar").hasClass("toggled")) {
        e.preventDefault();
      } else if ($(window).width() > 768) {
        var o = e.originalEvent.wheelDelta || -e.originalEvent.detail;
        this.scrollTop += 30 * (o < 0 ? 1 : -1);
        e.preventDefault();
      }
    });
  
    $(document).on("scroll", function() {
      if ($(this).scrollTop() > 100) {
        $(".scroll-to-top").fadeIn();
      } else {
        $(".scroll-to-top").fadeOut();
      }
    });
  
    $(document).on("click", "a.scroll-to-top", function(e) {
      var target = $(this);
      $("html, body").stop().animate({
        scrollTop: $(target.attr("href")).offset().top
      }, 1000, "easeInOutExpo");
      e.preventDefault();
    });
  })(jQuery);




// repeat the table rows
  
// Get the table body
var tableBody = document.querySelector("#dataTable tbody");

// Get the row to be repeated
var rowToRepeat = document.querySelector("#dataTable tbody tr");

// Loop to add 10 copies of the row
for (var i = 0; i < 10; i++) {
    // Clone the row
    var clonedRow = rowToRepeat.cloneNode(true);

    // Update any necessary values in the cloned row
    clonedRow.querySelector("td:nth-child(2)").textContent = i + 2;

    // Append the cloned row to the table body
    tableBody.appendChild(clonedRow);
}

